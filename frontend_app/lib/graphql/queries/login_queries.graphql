query FindFirstAuth($where: AuthWhereInput) {
  findFirstAuth(where: $where) {
    id
    password
  }
}

query FindFirstUsers($where: UsersWhereInput) {
  findFirstUsers(where: $where) {
    id
    name
    user_type_id
    user_type {
      name
    }
  }
}